# Zależności deweloperskie / CI (lint, format, testy).
# Instalacja:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

ruff>=0.6
pytest>=8.0
pytest-django>=4.8
mypy>=1.11
django-stubs[compatible-mypy]>=5.0
types-requests>=2.31
